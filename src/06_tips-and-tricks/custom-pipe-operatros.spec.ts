/*
a function that accepts an obs$ and returns a obs$ can be piped

or function accepts options object
*/
describe('custom pipeable operator', () => {

    test('test should follow', () => {

    });
});